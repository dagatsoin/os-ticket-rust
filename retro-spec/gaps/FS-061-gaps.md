# FS-061 Gap Report — Upgrader & Database Migration Streams

Phase-2 GAP-CLOSE run. Source slice: `scp/upgrade.php`, `setup/upgrade.php`, `include/class.upgrader.php`, `include/class.migrater.php`, `include/class.setup.php`, `include/ajax.upgrader.php`, `include/upgrader/*.inc.php`, `include/upgrader/streams/core/*` (patch/task/cleanup/sig), `scp/js/upgrader.js`, plus cross-ref `class.osticket.php` / `class.config.php` signature methods and `scp/admin.inc.php` / `scp/staff.inc.php` gating.

| id | type | severity | what the code does | what the spec said | fix applied |
|----|------|----------|--------------------|--------------------|-------------|
| G1 | INCORRECT | High | `check_mysql()` is purely `extension_loaded('mysql')` — no MySQL version comparison at all; only `check_php()` does a real `version_compare(PHP_VERSION, '4.3')`. The "v4.4" is a static screen label. | FS-061.9 & BS-061-13 described the MySQL check as "MySQL extension loaded ≥ v4.4", implying a 4.4 version gate. | Corrected FS-061.9 text; added FS-061.16 (check semantics), BS-061-18, KL-061.10, EC-061.19. |
| G2 | MISSING | High | The AJAX upgrade endpoint (`ajax.php/upgrader`) is routed **POST-only** and boots through `staff.inc.php`, which rejects any POST lacking a valid CSRF token with HTTP 400 "Valid CSRF Token Required" — so the AJAX path is CSRF-protected, not just the wizard forms. | FS-061.9 only said "All wizard POST forms carry a CSRF token"; AJAX CSRF/POST-only not stated. | Expanded FS-061.9 CSRF bullet; added BS-061-19. |
| G3 | IMPRECISE | Medium | Completion ticket hardcodes sender email `support@osticket.com`, name "osTicket Support", `topic_id=0`, random 6-digit `ticketID` (Misc::randNumber(6)), status `open`, source `Web`; first thread entry is type `M`, source `Web`, **title "osTicket Upgraded"** (no "!"), body from `msg/upgraded.txt` else literal "Congratulations and Thank you for choosing osTicket!". | FS-061.14/BS-061-16 said "default dept/priority", "subject 'osTicket Upgraded!'", "from 'osTicket Support'" — missed the hardcoded email, topic 0, random number, the distinct thread title, and the exact fallback body. | Rewrote FS-061.14 ticket bullets; added BS-061-20. |
| G4 | MISSING | Medium | Ticket creation is best-effort: if INSERT fails or no insert id, the thread message is skipped and the run still reaches `done`; failure never aborts. `setState('done')` is the trigger (fires once from either AJAX or manual path). | Not stated. | Added best-effort note to FS-061.14; added BS-061-21. |
| G5 | IMPRECISE | Medium | Most tasks are single-shot: base `MigrationTask` defaults `isFinished()`=true, `run()`/`sleep()`/`wakeup()` no-ops, `description`="[Unnamed task]", `status`="finished". Only `AttachmentMigrater` is resumable (sleep/wakeup, isFinished by queue length). DB-session/API-key/password/group-access tasks ignore `max_time` and finish in one call. | FS-061.5 / BS-061-09 presented all tasks as resumable batches. | Added single-shot clause to FS-061.5; added BS-061-22; added KL-061.12. |
| G6 | IMPRECISE | Low | AJAX 200 body after a patch batch is the literal `"Upgraded to <version> ... post-upgrade checks!"` (not getNextAction); task path returns getNextAction; 201 path calls `session_write_close()` before responding; JS waits ~3s then reloads with a cache-buster. | FS-061.11 said the 200 carries "the next-action label". | Refined FS-061.11 response-code bullets. |
| G7 | MISSING | Low | `scp/upgrade.php` POST handler: only runs when `!isAborted()`; only `prereq`/`upgrade` recognized, else `$errors['err']='Unknown action!'`; manual `upgrade` case explicitly sets `aborted` when `getErrors()` non-empty after the step. | Not documented. | Added FS-061.9 bullets (unknown action / aborted-POST no-op); added EC-061.15, EC-061.16, EC-061.17. |
| G8 | MISSING | Low | `set_time_limit(0)` runs unconditionally (safe-mode guarded) inside the StreamUpgrader constructor — once per discovered stream — not lazily before patching. | FS-061.4 noted the limit is removed "for the upgrade request" but not the per-stream-construction timing. | Added FS-061.17. |
| G9 | MISSING | Low | Bottom-of-file state switch (`default`) selects the prereq screen but emits "Nothing to do! System already upgraded to **<version>** with no pending patches to apply." — distinct wording from the POST-time "already upgraded to the current version". Order: aborted → rename → already-current → not-upgradable (`$cfg->getSchemaSignature()`). | EC-061.1 quoted only the already-current message generically. | Added EC-061.18 (and the bottom-switch ordering note implicit in EC-061.18). |
| G10 | IMPRECISE | Low | The linear migrater only inspects patches *leaving* a signature; multiple patches *converging onto* one destination are normal (shipped `core` has several, e.g. `15719536-dd0022fb` + `15b30765-dd0022fb`). A true fork (two patches leaving one signature) is what stops the walk. | KL-061.1/EC-061.3 framed the limitation around forks without distinguishing convergence. | Added KL-061.11. |

## Verification notes (spec confirmed CORRECT — no change)

- Signature comparison is case-insensitive (`strcasecmp` in `isUpgradePending`) — matches BS-061-01.
- `getSchemaSignature` namespace fallback chain (namespaced row → legacy single row → `md5(getDBVersion())`) — matches FS-061.1 / FS-061.10.
- `getUpgradeStreams` default to `core`, `#`-comment stripping, requires both `<stream>.sig` and `<stream>/`, static memoization — matches FS-061.2 / BS-061-15.
- ≤5 patches per request, 80%-of-max-time-box, task-breaks-batch, signature advanced per patch — matches FS-061.4 / BS-061-07 / BS-061-08 / BS-061-05.
- Cleanup non-aborting (`load_sql_file(..., $abort=false)`), "Unable to process cleanup file" log — matches FS-061.6 / BS-061-10.
- Bogus task ("Bogus migration task", non-string/missing class) — matches EC-061.5.
- AttachmentMigrater 1.5 (`{key}_{name}`) vs 1.6 (`{month}/{key}_{name}`) path probe, skip-list on missing, unlink-failure logged not retried (nonzero file_id prevents reprocessing), invalid upload dir aborts the batch — matches EC-061.7 / EC-061.8.
- `setup/upgrade.php` redirects to `scp/upgrade.php`; admin gate redirect excludes `upgrade.php`+`logs.php` — matches FS-061.9 / BS-061-02 / BS-061-03.
- onError: logs to system log (admin alerted), records error, sets state aborted, emails operator only when operator email differs from admin email, alert/default email then raw Mailer fallback, subject `[<stream>]: Upgrader Error` — matches FS-061.12 / BS-061-11 / BS-061-12.

## Summary

- Gaps found/fixed: **10** — INCORRECT ×1, IMPRECISE ×5, MISSING ×4. (Severity: High ×2, Medium ×3, Low ×5.)
- New spec elements: **FR +2** (FS-061.16, FS-061.17), **BS +5** (BS-061-18..22), **EC +5** (EC-061.15..19), **KL +3** (KL-061.10..12); plus in-place corrections to FS-061.9, FS-061.11, FS-061.14, FS-061.5, BS-061-13(label), KL-061.9.
