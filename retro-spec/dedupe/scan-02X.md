# DEDUPE scan — band FS-02X (FS-020 queue/dashboard/search, FS-021 ticket view/workflow, FS-022 canned/attachments)

Scope: scan-only. Recommendations are advisory; no spec text modified.
Convention: VERBATIM = near-identical restatement of mechanics/values; SUMMARY = acceptable cross-referenced paraphrase.

## Findings table

| ID | Content item | Recommended canonical owner (why) | Restating specs (refs) | Classification | Fix |
|----|--------------|-----------------------------------|------------------------|----------------|-----|
| D02X-1 | System overdue sweep: "up to 50 open not-yet-overdue tickets per run, oldest first" + the three reopened-aware qualification conditions (never-reopened≥grace from created / reopened≥grace from reopened / explicit due date past) | **FS-032.11** owns SLA→due-date derivation & sweep selection math (config-driven); **FS-043 BS-432** owns the cron job inventory + cadence. FS-021 owns only the `markOverdue`/`checkOverdue` *action* + activity-note text. | FS-021.13 + BS-021.17; FS-032.11 (lines 460-466); FS-043.304-307 + BS-432 (456-467) | **VERBATIM** (triple: the 50-cap and the 3 sweep conditions appear in all three) | TRIM FS-021.13 sweep-eligibility detail to a cross-ref to FS-032.11/FS-043 BS-432; keep FS-021's action note text + idempotency. FS-032 and FS-043 already cross-ref each other loosely — pick FS-032.11 as math owner, FS-043 BS-432 as cadence owner, and have FS-021 reference both (it currently restates them). |
| D02X-2 | Per-ticket Ban / Unban staff action: permission (`canBanEmails`), "Email already in banlist" / "Email (<addr>) added to banlist" / "Email removed from banlist" / "Email is not in the banlist", unbannable-only-if-explicit-entry rule | **FS-042.12** is the declared canonical owner (banlist storage + per-ticket ban action). | FS-021.14 (full restatement) + BS-021.11; FS-042.12 (lines 197-208) | **VERBATIM** (FS-021.14 and FS-042.12 state the same messages + unbannable rule) | TRIM FS-021.14 to a cross-ref to FS-042.12 for the ban/unban mechanics; FS-021 keeps only that the reply form is blocked while banned (workflow consequence). FS-021.14 already has a ">See FS-042" pointer but still restates the messages. |
| D02X-3 | Reply-blocked-while-banned + ticket-view ban banner wording. Two different message strings ("Email is in banlist. Must be removed to reply." enforcing vs "Email is in banlist! Must be removed before any reply/response" banner) | Enforcement = **FS-042.10/.12**; banner+reply-block-as-workflow = **FS-021** (it owns `postReply`). The wording-divergence note is worth keeping in ONE place. | FS-021.3 + FS-021.23a + BS-021.11; FS-042.136-137, FS-042.206 | **VERBATIM** (the banner string + the divergence observation appear in both) | KEEP in FS-021 (reply action consequence) but TRIM the enforcing-check detail to cross-ref FS-042.10. FS-042.137 already says "the banner is informational, this check is enforcing" — let FS-042 own the divergence note, FS-021 cross-ref it. |
| D02X-4 | Estimated due date = explicit due date else `created + SLA grace_period hours`; overdue measured against effective due date (reopened-aware) | **FS-032.11** (SLA due-date derivation is config/plan-owned). | FS-021.13 + BS-021.6; FS-032.11 (455-466) | **VERBATIM** (formula restated identically) | TRIM FS-021 BS-021.6 to cross-ref FS-032.11; FS-021 keeps the per-ticket "operator may override via explicit due date" workflow framing only. |
| D02X-5 | SLA selection precedence: trump > department > help topic > system default (`selectSLAId`) | Split: precedence ranking sits naturally with **FS-032** (SLA plans) / **FS-030** (dept+topic defaults); FS-021 owns *when* re-selection fires (transfer/edit). | FS-021.13 + BS-021.7; FS-030/FS-032 (defaults). FS-032.11 references but does not fully enumerate the precedence chain. | **SUMMARY-bordering-VERBATIM** (precedence list is in FS-021 only at full detail; mild) | KEEP in FS-021 as the workflow trigger, but consider moving the precedence ranking to FS-032 and cross-ref. Low priority — FS-021 is currently the only full statement. |
| D02X-6 | `mass_process` handler (reopen/close/mark_overdue/delete): gating `canManageTickets`, per-action re-check, all/partial/none result messages | Genuine FS-020↔FS-021 seam. **FS-020.9** owns selection/gating/queue→action mapping; **FS-021.18/.21** owns the mutation logic. Both already declare this split explicitly. | FS-020.9 + BS-020.9/10/11; FS-021.18 + FS-021.21 | **SUMMARY** (intentional, well-bounded, each side states ownership) | KEEP both — boundary is correctly drawn and labeled. No action. |
| D02X-7 | `checkStaffAccess` per-ticket access rule (dept-access OR open+assigned/team) vs queue listing visibility predicate | Per-ticket = **FS-021.2**; listing scope = **FS-020.4/BS-020.2**; underlying access-limited/dept-access primitives = **FS-002/FS-031**. | FS-020.4 + BS-020.2; FS-021.2 + BS-021.2; FS-002.218-223, .447 | **SUMMARY** (deliberately distinct: listing OR-predicate vs single-ticket gate; both cross-ref FS-002/031) | KEEP — distinct surfaces. Both correctly defer the access model to FS-002/FS-031. No action. |
| D02X-8 | Staff "show assigned tickets" override effective only for admin/dept-manager | **FS-002** owns the access/flag-honouring primitive (FS-002 line 200). | FS-020 BS-020.18 + BS-020.4; FS-002.200 | **VERBATIM** (the admin-or-manager gate stated in both) | TRIM FS-020 BS-020.18 to cross-ref FS-002; FS-020 keeps only how the override feeds the Open-queue toggle + the quick-stat branch (BS-020.17), which is genuinely FS-020's. |
| D02X-9 | `%{...}` variable substitution into canned-response body + `%{recipient}` post-pass | **FS-040.11** is declared canonical (VariableReplacer grammar). | FS-021.16 (lists which template per event), FS-022.14 (canned body substitution); FS-040.11 + BS-040.20, FS-040.483 | **SUMMARY** (FS-021/FS-022 correctly defer grammar to FS-040; only state *that* substitution happens) | KEEP — clean. FS-022.14 and FS-021.16 both carry ">owned by FS-040" pointers. No action. |
| D02X-10 | Content-addressed chunked file store + orphan reclamation (`ft='T'` scoped, ref-counted purge) | **FS-022.12/.5/BS-022.10/11** is declared canonical file-store owner. | FS-022 (owner); FS-043.312-313 (cron orphan cleanup), FS-050.127-129, FS-011.495, FS-010.115/480 | **SUMMARY** (all consumers cross-ref FS-022; FS-043 restates the ref-count predicate but cites FS-022) | KEEP. Minor: FS-043.312 restates the "no ticket/canned/FAQ reference + ft=ticket" predicate verbatim-ish — could TRIM to cross-ref FS-022 BS-022.10/11. Low priority. |
| D02X-11 | Attachment upload validation (allowed-types extension match, max_file_size) | **FS-022.13/BS-022.13/14** canonical (shared format helper). | FS-022 (owner); FS-011.112, FS-041.118/BS-041.11 | **SUMMARY** (consumers cross-ref FS-022/FS-032 for config) | KEEP — clean cross-refs. No action. |
| D02X-12 | Attachment download hash forms (`md5(file_id+session_id+file_hash)`, 64-char combined `getDownloadHash`) + `file.php?h=` link | **FS-022.10/.12** canonical (Phase-1 recommended FS-022 as attachment-download owner). | FS-022 (owner); FS-050.131 (`file.php?h=` + `md5(id+session_id+hash)`), FS-010.96-115 (client session hash — distinct), FS-040 (none) | **SUMMARY** (FS-050 states the hash form as a one-line summary w/ cross-ref) | KEEP. FS-050.131 restates the md5 hash composition — acceptable as summary but could TRIM to "(hash composition owned by FS-022)". Low priority. |
| D02X-13 | Dashboard tabular report CSV/JSON export mechanics | **FS-090.26** owns the export/encoder mechanic; **FS-020.13** owns the data/grouping/columns. Bidirectional cross-refs present. | FS-020.13 + BS-020.15; FS-090.26 + FS-090.337 | **SUMMARY** (intentional, cleanly bounded — FS-090 explicitly disclaims data ownership, FS-020 disclaims mechanics) | KEEP — exemplary split. No action. |
| D02X-14 | Ticket CSV export (`Export::saveTickets`, `tickets-YYYYMMDD.csv`, query-token mechanics) + the export column set | **FS-090** owns export mechanics + column map (FS-090.24/.25); **FS-020.10** owns the session query-token storage + which query is exported. | FS-020.10 + BS-020.12; FS-021.1/.36 (entry-script export branch); FS-090.24/.25 | **SUMMARY** (FS-021.1 duplicates the export error-string set that FS-020.10 also has, because of the shared entry script) | MINOR: FS-021.1 restates the "Query token required / not found / Unable to dump" strings that FS-020.10/EC-020.5 own. TRIM FS-021.1 export branch to a one-line cross-ref to FS-020.10 (it shares scp/tickets.php but the export UI is FS-020's). |
| D02X-15 | Quick ticket stats (`getStaffStats`) driving submenu counts + >10 assigned/overdue warnings | **FS-020.11** is canonical (declared owner, despite living in `class.ticket`). | FS-020.11 + BS-020.14/17/18; FS-021.1 (renders the same submenu + same >10 warning text); FS-090.708 | **VERBATIM** (FS-021.1 repeats the submenu list + the ">10 assigned" warning string) | TRIM FS-021.1's submenu/quick-stat paragraph to cross-ref FS-020.2/FS-020.11 (FS-021.1 already says "submenu listing itself is shared with FS-020" but still restates the warning). |
| D02X-16 | `postMessage` thread-append: owner→Message / staff→Note entry-type decision; closed-ticket reopen-on-message (auto-response exempt) | Thread-model entry-type decision = **FS-021.22/.5** (declared thread-model owner); inbound *trigger* = **FS-041.6/BS-041.8**. | FS-021.5 + EC-021.17 + BS-021.5; FS-041.142, FS-041.6 | **SUMMARY** (FS-021 owns the rule, FS-041 owns the trigger; both labeled) | KEEP — correctly split. No action. |
| D02X-17 | Canned-response auto-reply leaves ticket unanswered + disables standard auto-response (filter-set canned) | Canned-as-filter-action = **FS-042.86/129**; canned posting semantics (poster "SYSTEM (Canned Reply)", unanswered) = **FS-022.14/BS-022.15**. | FS-022.14 + BS-022.15; FS-042.86/129; FS-011.189; FS-021.16 (auto-response suppression clause d) | **SUMMARY** (each states its own slice; mild overlap on "leaves unanswered") | KEEP — boundaries acceptable. The "leaves ticket unanswered" appears in FS-022/FS-042/FS-011/FS-021; canonical = FS-022.14 (canned posting contract). Optionally TRIM the others to cross-ref. Low priority. |

## Inverse check — content in FS-02X that may belong elsewhere

- **D02X-1/D02X-4 (overdue sweep + due-date formula in FS-021):** belongs to FS-032/FS-043 — flagged above (TRIM).
- **D02X-2 (ban/unban mechanics in FS-021.14):** belongs to FS-042 — flagged (TRIM).
- **FS-021.1 CSV export branch + quick-stats submenu (D02X-14/15):** queue/listing material that belongs to FS-020 — flagged (TRIM). Hosted in FS-021 only because of the shared `scp/tickets.php` entry script.
- **FS-020 dashboard CSV (D02X-13):** export *mechanic* correctly already deferred to FS-090. Clean.
- No alert-template content (FS-040) or filter/ban-storage content (FS-042) is canonically misplaced *inside* FS-02X beyond the ban-action restatement (D02X-2/3).

## Notes
- The FS-020↔FS-021 mass_process seam (D02X-6) and the FS-020/FS-090 dashboard-export seam (D02X-13) are model examples of clean, bidirectionally-labeled splits — no change needed.
- The biggest concrete redundancy is the **overdue-sweep math stated 3×** (D02X-1) and the **ban/unban action stated 2×** (D02X-2), both fully VERBATIM.
