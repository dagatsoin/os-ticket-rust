# Phase-4 DEDUPE scan — band FS-04X (FS-040 / FS-041 / FS-042 / FS-043)

Scope: email accounts/templates/outbound (FS-040), inbound pipeline fetch/pipe/parse (FS-041),
filters/banlist/inbound routing (FS-042), API/cron (FS-043). Scan only — nothing modified.

Legend — **Fix**: `trim-to-crossref` = delete the restated detail, leave a one-line pointer;
`merge` = consolidate into the canonical owner; `keep` = acceptable summary/attributed reference,
no action. **Type**: VERBATIM = catalog/table/enum copied near-word-for-word; SUMMARY = paraphrase
with attribution.

| ID | Content item | Canonical owner (why) | Restating spec(s) + ref | Type | Fix |
|----|--------------|------------------------|--------------------------|------|-----|
| D04X-1 | Email template **type catalog** (13 code-names → display name + description) | **FS-091** (canonical data-model/seed owner; `all_names` is a stored application constant) | FS-040 §"Template Type Catalog" (Data Req, 13-row table BS-040.7); FS-091.7 (lines 92–106, 13-row table); FS-091 Seeded Email Templates table (lines 455–467) | VERBATIM (FS-040 table ≈ FS-091.7 table, same code-names + descriptions) | trim-to-crossref: FS-040 keeps the behavioral resolver rules (BS-040.7/BS-040.13 fallback) but should cite FS-091.7 for the catalog rows rather than re-tabulating all 13. Both already cross-link; the table itself is the duplication. |
| D04X-2 | `%{token}` **variable catalog** (base ticket vars, expandable objects, context vars) | **FS-040** (canonical — owns VariableReplacer grammar + catalog; FS-040.11 / Data Req "Variable Catalog") | FS-033.9 (lines 256–266: Base + Other Variables reference card, ~16 tokens enumerated); FS-091 (line 110 — pointer only, clean); FS-022 (pointer only, clean) | VERBATIM-ish (FS-033.9 re-lists `%{ticket.*}` + context tokens) but **legitimately scoped** — FS-033.9 owns only the *static content of the reference-card HTML fragment* and explicitly defers resolution to FS-040 (note at lines 271–273) | keep: FS-033 owns the card-content artifact; it correctly cross-refs FS-040 as the substitution authority. Acceptable. |
| D04X-3 | `%{token}` **grammar** (dot-path, accessor convention, unknown-token-preserved) | **FS-040** (FS-040.11 / BS-040.14–BS-040.20) | FS-022 (lines 10, 36, 317, 660: "supports ticket-variable substitution", all attributed to FS-040); FS-033.9 note | SUMMARY (mentions only, no grammar restated) | keep — clean attributed references. |
| D04X-4 | Filter **operator** + **criterion** enums (storage values `equal/not_equal/contains/dn_contain/starts/ends`; `name/email/subject/body/header`) and admin labels | **Split**: FS-091.9 owns the *storage enum sets* (canonical data model); **FS-042** owns the *behavioral semantics + editor labels* (FS-042.3/FS-042.4) | FS-091.9 (lines 122–128: enums + `getSupportedMatchTypes` labels); FS-042.3 (lines 44–61: same enums + same labels); FS-091 entity #14 filter_rule (line 351) | VERBATIM (operator value→label list duplicated in FS-091.9 and FS-042.3) | trim-to-crossref: FS-042.3 may keep operator *semantics* (what `contains`/`ends` mean for matching) but the value→storage-enum + the label map duplicates FS-091.9. Have FS-042 cite FS-091.9 for the enum/label canon, retain only semantics. (FS-042.3 already notes "enum sets owned by FS-091".) |
| D04X-5 | **Auto-response / auto-bounce header marker vocabulary** (Auto-Submitted, Precedence bulk/junk/list, X-Auto-Response-Suppress, mailer-daemon From, delivery-failure subjects, etc.) | **FS-042** (FS-042.9 declares itself "the canonical source for the marker vocabulary"; markers live on the filter subsystem class) | FS-041 BS-041.12 (lines 264–266) + BS-041.13 (lines 268–271) restate the **full marker list verbatim**; FS-040 BS-040.22 (line 176) lists the *outbound* suppression headers it emits | VERBATIM (FS-041 BS-041.12/13 enumerate the same markers as FS-042.9) | trim-to-crossref: FS-041 is the *consumer* per the Phase-1 seam; BS-041.12/BS-041.13 should cite FS-042.9 for the marker list and keep only the pipeline-side *behavior* (suppress autoresponse / drop bounce on create-failure). The drop-timing prose (BS-041.13) is genuinely FS-041's and stays. FS-040 BS-040.22 is the *outbound-emission* side (distinct concern) — keep. |
| D04X-6 | **Autoresponse suppression triggers** (system-own-address, detected auto-response, `mailer-daemon@`/`postmaster@`, canned-reply-posted) | **FS-041** for the email-pipeline detection (FS-041.7 / BS-041.12); FS-042.9 for the header heuristic; **FS-021** owns the alert/auto-response *send-decision* | FS-011 §FS-011.12 + line 191 (attributed to FS-041); FS-021 FS-021.226 + BS-021.20 + EC-021.20 (lines 226, 436–439, 596–598); FS-042 EC-042-17 | SUMMARY (FS-011 attributes to FS-041; FS-021 owns the send path so states it as its own rule) | keep — FS-021 legitimately owns the `onNewTicket/onMessage` send-decision and must state the suppression gate; it cross-refs FS-041 for the header-detection source. FS-011 attributes. No trim needed, but note the triple-statement (FS-011/FS-021/FS-041/FS-042) of the same 4 triggers is the most-spread item in the codebase — worth a single canonical "suppression triggers" anchor in FS-041 that the others cite. |
| D04X-7 | **Pipe exit-code map** (internal outcome → BSD-sendmail exit code: 0/65/66/69/75/77) | **FS-041** (Phase-1 seam: FS-041 canonical for parse/threading/pipe mechanics; BS-041.2 full table) | FS-041 BS-041.2 (lines 202–214, full table); FS-043 FS-043.15 (lines 225–240) + BS-440 (lines 514–520) restate the same code map | VERBATIM (both give the full 201→0 / 400→66 / 401-403→77 / 415-417/501→65 / 503→69 / 500→75 mapping) — **the explicit D-check item (FS-041.1/BS-041.2 vs FS-043.15)** | trim-to-crossref: FS-043.15 is explicitly framed as "(Reference)" and "API-level response mapping... canonically owned by FS-041" (line 229–230) — so it already concedes ownership but **still restates the whole table**. Trim FS-043.15/BS-440 to cite BS-041.2 and keep only "the pipe controller extends the API controller + requires no key + maps via the FS-041 table". |
| D04X-8 | **Email-format API path: reply-detection + default fill-in** (force source=Email, subject/message/emailId defaults, strip priority; ticketId→header→new-ticket threading) | **FS-041** (Phase-1 seam: FS-041 canonical for parse/threading; FS-041.5.3 post-parse fixup + FS-041.6 threading order) | FS-043 FS-043.4 (email bullet, lines 174–178), FS-043.14 (lines 203–223), BS-439 (lines 504–512); FS-041 FS-041.5.3 + FS-041.6 own the same fixup + threading order | VERBATIM-ish (FS-043.14 re-describes the exact fixup defaults and the ticketId→headers→new-ticket precedence that FS-041.5.3/FS-041.6 own) | trim-to-crossref: FS-043 should own only the *HTTP wire framing* (format dispatch, 201 response, field allow-list) and cite FS-041.5.3/FS-041.6 for the fixup defaults + threading precedence. FS-043.14 currently duplicates the normalization rules. (FS-043 does say "threading mechanics → FS-041", but still restates the defaults.) |
| D04X-9 | **HTTP-email permitted field allow-list** (base 13 fields + email-extension 8 fields) | **Shared API contract**; FS-043 owns the *wire allow-list* (FS-043.4), FS-041 owns the *parsed-record field set* (FS-041.5.1) | FS-041 FS-041.5.1 (lines 104–105); FS-043 FS-043.4 (lines 184–188) + Data Req (lines 538–544) | VERBATIM (identical base field list + identical email-extension list in both) | trim-to-crossref: pick one canonical (recommend **FS-043.4**, the API endpoint owner, since the allow-list is an API-validation concern) and have FS-041.5.1 cite it. They are byte-identical lists. |
| D04X-10 | **Mail-fetch cadence / error-backoff** (poll only-if-enabled + IMAP-required; eligibility = active AND due AND not-in-backoff; 5 consecutive errors → 10-min backoff + admin alert; oldest-last-fetch-first; ~80% exec-time box; max_fetch cap + 80% circuit-breaker) | **FS-041** (FS-041.3 + BS-041.5/BS-041.6/BS-041.15 own the fetch *execution*) | FS-043 FS-043.8 (lines 316–336) + BS-434 (lines 475–480) restate the whole eligibility/backoff/time-box; FS-032 (lines 260–263: `enable_mail_polling`/`enable_auto_cron` config — config-owner, clean) | VERBATIM-ish (FS-043.8 re-specifies the 5/10-min/80%/oldest-first rules that BS-041.5/6/15 own) | trim-to-crossref: FS-043 is the *trigger/scheduler* — it should own only "cron invokes mail-fetch as job #1 on its cadence" and cite FS-041 for eligibility/backoff/time-box mechanics. FS-043.8 currently re-derives FS-041's numbers. (FS-043.8 does append "Full IMAP/POP3 fetch mechanics → FS-041" but still restates the gating.) |
| D04X-11 | **Cron job inventory** (mail-fetch → ticket-monitor[overdue+lock-cleanup] → purge-logs → clean-orphans) | **FS-043** (FS-043.7 — canonical cron inventory owner) | FS-033.7 (lines 201–224: purge-logs job, attributed "see FS-043 for the full cron task set"); FS-032 (overdue sweep consumed "by FS-021/FS-043"); FS-021 (line 621: "cron invokes checkOverdue and lock cleanup"); FS-041 (mail-fetch triggered by cron) | SUMMARY — each domain spec names only *its own* job and points back to FS-043 for the full inventory | keep — clean ownership split: FS-043 owns the inventory; FS-033 owns purge-logs *body*, FS-021 owns overdue/lock *body*, FS-041 owns mail-fetch *body*, FS-022 owns orphan-cleanup *body*. All cross-ref correctly. This is the intended seam, not duplication. |
| D04X-12 | **Ticket-overdue qualification** (SLA grace from creation/reopen, explicit due date, 50/run cap) | **FS-032** owns the SLA grace/overdue *computation* (FS-032.11/BS-032.10); **FS-043** owns the *sweep job* (BS-432) selecting/capping; FS-021 owns the overdue *alert send* | FS-043 BS-432 (lines 456–467) restates the (a)/(b)/(c) qualification; FS-032.11 (lines 448–470) owns the reopen-aware computation; FS-021 §onOverdue | SUMMARY/partial-VERBATIM (BS-432 re-derives the grace-from-reopen rule that FS-032.11 owns) | trim-to-crossref: FS-043 BS-432 should keep the *sweep selection + 50-cap + "Ticket Marked Overdue" log* and cite FS-032.11 for the grace-period/reopen qualification math. (FS-043 already says "see BS-432" internally; the cross-spec owner is FS-032.) |
| D04X-13 | **SYSTEM BAN LIST reserved-filter definition** (exec order 99, reject-on-match, match-any, auto-created, undeletable) | **FS-042** (FS-042.11/BS-042-13 — owns banlist behavior); **FS-091** owns the *seed row* (BS-091.9) | FS-091 BS-091.9 (lines 273–281) + EC-091.8 (lines 604–605) + Seeded Filter (line 449); FS-042 BS-042-13/BS-042-24 | SUMMARY (FS-091 owns the install-seed fact; FS-042 owns the runtime behavior) | keep — legitimate split: FS-091 = "it is seeded with these values"; FS-042 = "how the banlist behaves at runtime". Both attribute. No action. |
| D04X-14 | **Mail-account fetch settings consumed** (host/port/protocol/encryption/freq/max-fetch/archive/delete + error bookkeeping) | **FS-040** (owns the account record + save-time settings) | FS-041 Data Req "Mail-Account Settings Consumed" (line 328, attributed to FS-040); FS-043 Data Req (lines 545–547, attributed to FS-040/FS-041) | SUMMARY — both explicitly attribute to FS-040 and list only the fields they *read* | keep — clean consumer references. |
| D04X-15 | **Thread-entry synthetic message-id + threading-header matching** (`<rand24@md5-of-url>`; References/In-Reply-To then `#NUMBER` subject gated on owner email) | **FS-041** (FS-041.6/BS-041.7/BS-041.18 own threading detection + backfill) | FS-021 (lines 308, 310–311, 315: thread-entry model restates synthetic-id form + matching order, attributed "lookupByEmailHeaders/postEmail consumed by FS-041") | SUMMARY (FS-021 owns the thread-entry *storage model* so states the id-generation; cross-refs FS-041 for matching) | keep — FS-021 owns the thread/ThreadEntry table and must describe the message-id column it backfills; it attributes the *matching* to FS-041. Minor overlap on the `#NUMBER` rule but acceptable (FS-021 owns the model, FS-041 owns the lookup). |
| D04X-16 | **X-Forwarded-For → REMOTE_ADDR rewrite; is_cli / get_path_info** (used by API key IP-binding + pipe/cron CLI gating + dispatcher) | **FS-001** (FS-001.2 bootstrap; FS-001 §is_cli/get_path_info) | FS-043 FS-043.6 (line 287, "mechanism owned by FS-001"), FS-043.10 (CLI detection), FS-043.3 (path-info); FS-001 lines 207–209, 566–568 | SUMMARY — FS-043 attributes all three to FS-001 | keep — clean. FS-001 owns; FS-043 consumes with attribution. |

## Summary

- **Findings: 16 rows.** VERBATIM (or near-verbatim catalog/enum/table copies needing trim-or-merge): **8** — D04X-1, D04X-4, D04X-5, D04X-7, D04X-8, D04X-9, D04X-10, D04X-12. Acceptable attributed summaries / legitimate ownership splits (keep): **8** — D04X-2, D04X-3, D04X-6, D04X-11, D04X-13, D04X-14, D04X-15, D04X-16.
- No outright contradictions found; every restating spec already carries a cross-reference. The defects are duplicated *content* (tables/enum lists/numeric thresholds) that should collapse to a single owner + pointer.

### 5 most significant

1. **D04X-7 — Pipe exit-code map (FS-041.1/BS-041.2 ↔ FS-043.15/BS-440).** The named D-check. FS-043.15 already concedes "canonically owned by FS-041" yet restates the entire 6-row exit-code table. → Owner **FS-041 (BS-041.2)**; trim FS-043.15/BS-440 to a cross-ref + the API-controller-specialization note only.
2. **D04X-5 — Auto-response/bounce marker vocabulary (FS-042.9 ↔ FS-041 BS-041.12/13).** FS-042.9 self-declares canonical; FS-041 re-enumerates the full marker list verbatim. → Owner **FS-042.9**; trim FS-041 BS-041.12/13 to cite it and retain only pipeline-side suppression/drop-timing behavior.
3. **D04X-8 — Email-format reply-detection + fixup defaults (FS-043.14/BS-439 ↔ FS-041.5.3/FS-041.6).** Phase-1 seam (FS-041 canonical for parse/threading). FS-043.14 restates the fixup defaults + ticketId→header→new precedence. → Owner **FS-041**; FS-043 keeps only HTTP wire framing + 201 contract.
4. **D04X-1 — Email template type catalog (FS-040 ↔ FS-091.7).** 13-row code-name/description table duplicated. → Owner **FS-091.7** (data-model/seed canon); FS-040 keeps resolver behavior (BS-040.7/BS-040.13) + cites the catalog.
5. **D04X-10 — Mail-fetch cadence/backoff (FS-043.8/BS-434 ↔ FS-041.3/BS-041.5/6/15).** FS-043 (scheduler) re-derives FS-041's 5-error / 10-min / 80%-time-box / max_fetch numbers. → Owner **FS-041**; FS-043 keeps only "cron runs mail-fetch as job #1" + cite.

(Honorable mention: **D04X-4** filter operator/criterion enum+label map duplicated FS-091.9 ↔ FS-042.3 — collapse the enum/label canon to FS-091.9, leave semantics in FS-042.3.)
