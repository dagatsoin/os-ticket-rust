# DEDUPE Scan — bands FS-05X + FS-06X + FS-092

Perspective specs: FS-050 (KB/FAQ), FS-060 (installer), FS-061 (upgrader), FS-092 (CLI/deploy/packaging).
Scan only — no specs modified. VERBATIM = literal value/string/list restated; SUMMARY = acceptable
behavioral restatement under an explicit cross-ref.

## Verdict on the known seams

| Seam | Held? | Notes |
|------|-------|-------|
| FS-032 category CRUD vs FS-050 KB-grouping | HELD | FS-050 Overview/dep-table explicitly cedes Category CRUD to FS-032; FS-050 consumes `Category` as grouping only. Minor overlap = the `Category::lookup` hollow-object quirk documented in BOTH (KL-050.8 / FS-032 note + KL-032.11) and the delete-cascade documented in both (BS-050.5 / FS-032.16). |
| Seed VALUE tables ownership FS-060 ↔ FS-091 ↔ FS-032 | MOSTLY HELD, leak in FS-060 | FS-091 is canonical seed-value owner; FS-060 restates many seed *values* verbatim (priorities w/ colors, SLA grace=48, 12 templates, groups perms) under a "seed data the installer writes" framing. Some are over-detailed (verbatim color hexes, urgency ranks) and duplicate FS-091 literals. |
| SetupWizard base FS-060 ↔ FS-061 StreamUpgrader | HELD | FS-061 cross-refs the shared SQL-loader / prereq / abort plumbing to FS-060; documents only the upgrade-specific extension. Prereq-contract values restated but flagged shared. |
| schema-signature mechanics FS-060 ↔ FS-061 ↔ FS-001 | HELD (clean split) | FS-001 owns the online/offline *gate* + 3-tier fallback lookup; FS-061 owns *detection/chaining*; FS-060 owns *install-time recording*. Each cross-refs the others. 3-tier fallback lookup is the one item restated in all three. |
| backup dump format FS-090.27 (export) ↔ FS-092 (import) ↔ FS-061 (manifest consumer) | HELD | FS-092 explicitly cedes format to FS-090.27, documents consume side only; FS-061 only consumes the stream-list. |
| enable_kb config FS-032 ↔ FS-050 | HELD | FS-050 consumes effect; FS-032/FS-091 own persistence/default. |
| ajax.kbase cannedResp FS-050 ↔ FS-022 | HELD | FS-050 notes co-location only; FS-022.14 owns the contract. |

## Findings

| ID | Content item | Recommended canonical owner (why) | Restating spec(s) + ref | V/S | Fix |
|----|--------------|-----------------------------------|-------------------------|-----|-----|
| D05X-1 | Seeded priorities full literal table — tags low/normal/high/emergency, colors `#DDFFDD`/`#FFFFF0`/`#FEE7E7`, urgency 1–4, ispublic, default_priority_id=2 | FS-091 (canonical seed reference rows; FS-091 §Seeded Priorities + BS-091.3) | FS-060 Data Req "Priorities (4)" restates every tag/color/urgency/public flag VERBATIM; FS-032.8 also restates the priority seed set | V | TRIM FS-060 to "4 priorities — full values in FS-091 §Seeded Priorities"; keep only the install-binding fact (default_priority_id wiring). FS-032.8 already cross-refs FS-091 — acceptable. |
| D05X-2 | Seeded SLA literal — "Default SLA", grace_period=48 hrs, active, escalation on, overdue-alerts not disabled | FS-091 (§Seeded SLA + FS-091.6) | FS-060 Data Req "SLA (1)" VERBATIM (grace_period=48); FS-032.11 restates grace semantics | V (FS-060) / S (FS-032) | TRIM FS-060 SLA seed line to a cross-ref; FS-032's overdue-computation semantics are its own (keep). |
| D05X-3 | 12 default email-template code-names + group name "osTicket Default Template" | FS-091 (FS-091.7 + §Seeded Email Templates, incl. 13th staff.pwreset note) | FS-060 Data Req "Email template group (1) … 12 templates" lists all 12 code-names VERBATIM | V | TRIM FS-060 to "the 12-template default group (FS-091.7)"; the full code-name list belongs to FS-091. FS-060 mail/template ownership already cross-refs FS-040/FS-091. |
| D05X-4 | Seeded groups + per-action permission flags (Admins/Managers/Staff, manage-faq off for Staff) | FS-091 (§Seeded Groups + BS-091.8 Cartesian access) | FS-060 Data Req "Groups (3) + permission flags" restates flag matrix; FS-091 §Seeded Groups is more precise (notes the post_ticket_reply/view_staff_stats default quirk) | V | TRIM FS-060 group-flag enumeration to cross-ref FS-091; FS-091's version is authoritative & richer. |
| D05X-5 | Cartesian group↔dept access seed ("every group → every dept") | FS-091 (BS-091.8) | FS-060 Data Req "Group↔department access" + FS-060.7 AC | S | KEEP as summary (1 line, already behavioral) — add `(BS-091.8)` cross-ref if not present. |
| D05X-6 | SYSTEM BAN LIST seed filter (execorder 99, reject-on-match) + sample rule `email equal test@example.com` | FS-091 (BS-091.9 + §Seeded Filter) — also FS-042 owns banlist mechanism | FS-060 Data Req "Ban-list filter (1)" VERBATIM (execorder 99, test@example.com) | V | TRIM FS-060 to cross-ref FS-091/FS-042; the literal exec order + sample email are FS-091's. |
| D05X-7 | Sample file `osTicket.txt` (text/plain, 25 bytes, "Canned attachments rock!") + canned_attachment link | FS-091 (FS-091.14 + §Seeded File) | FS-060 Data Req "Sample file (1)"; FS-022 owns canned/file storage | V | TRIM FS-060 to cross-ref FS-091.14. |
| D05X-8 | Timezone seed (30 rows, -12.0…+12.0, Eastern=-5.0) | FS-091 (§Seeded Timezones) | FS-060 Data Req "Timezones (30)" + BS-060-11 (Eastern lookup) | S | KEEP — FS-060's interest is the install-time binding (`offset=-5.0` lookup for admin row), which is genuinely installer behavior; just ensure cross-ref to FS-091 for the list. |
| D05X-9 | ~90 core config defaults (isonline=0, max_file_size=1048576, staff_max_logins=4, allowed_filetypes='.doc, .pdf', etc.) | FS-091 (§Installed Configuration Keys & Defaults) | FS-060 Data Req "Config (core namespace): ~90 default rows" — samples a handful VERBATIM but already says "(Full key list owned by FS-091/FS-032.)" | S | KEEP — already cross-ref'd; the handful of examples is acceptable illustration. No action. |
| D06X-1 | 3-tier schema-signature fallback lookup (namespaced → legacy single-row → md5 of ostversion) | FS-061 (FS-061.1/FS-061.10 — authoritative detection) per its own dep note | FS-001 §94-95 restates all 3 tiers; FS-061.1 last bullet restates; FS-060 references the recorded `.sig` | S (×3) | KEEP all three as summaries — each frames it for its own concern (FS-001=gate, FS-061=detection, FS-060=record). FS-001 already says "detail owned by FS-061". Acceptable triangulation, not dup. |
| D06X-2 | Prereq contract `{php:4.3, mysql:4.4}` + "MySQL check is driver-presence only, no version compare" | FS-061 (FS-061.16/BS-061-18 — most detailed) OR FS-060.2 (install-time) | FS-060.2 + EC-060-01/02 AND FS-061.16/BS-061-18/KL-061.10 both document the "label says 4.4 but only checks extension-loaded" quirk | S (both) | KEEP — genuinely shared contract; both cross-ref each other (FS-061.16 "shared with FS-060"). Each owns its own gate's behavior. No merge. |
| D06X-3 | SQL-loader mechanics (strip `#`/`--` comments, `%TABLE_PREFIX%` substitute, SQL_MODE="", split on `;`, abort on fail) | FS-060 (FS-060.7 — the loader lives in the installer/setup base) | FS-061.4 AC restates the loader steps | S | KEEP — FS-061 cross-refs "(cross-ref FS-060 SQL-loader)"; restatement is a 1-line summary. Acceptable. |
| D06X-4 | streams.cfg discovery rule (one stream/line, `#` comments, require `<stream>.sig`+`<stream>/`, default `core`) | FS-061 (FS-061.2/BS-061-15 — stream subsystem owner) | FS-060.7 + BS-060-10 restate the same discovery rule VERBATIM (incl. default-core fallback) | V | TRIM FS-060 to "stream list per FS-061.2 (default `core`)"; FS-060 only needs the install-time signature-record fact. The discovery algorithm is FS-061's. |
| D06X-5 | Completion/welcome ticket synthetic-identity fields (status open, source Web, sender support@osticket.com, "osTicket Support", topic_id 0, random 6-digit no.) | Pattern is shared but each instance is distinct ticket; FS-091 owns ticket field enums | FS-060 (BS-060-18/EC-060-14/KL-060-11 "osTicket Installed!") AND FS-061 (FS-061.14/BS-061-20/KL-061.9 "osTicket Upgraded!") — near-identical synthetic-identity recipe | S (both) | KEEP both — they are two *different* seeded tickets (install vs upgrade) with parallel recipes; not a dup. Optionally add a mutual "mirrors FS-060 welcome ticket" note for discoverability. No trim. |
| D06X-6 | Schema-signature md5 algorithm hard-coded; per-stream config TODO | FS-061 (KL-061.7) and FS-060 (KL-060-10) | Both KLs say the same thing for their own load path | S | KEEP — each KL scoped to its own subsystem's verifier. Acceptable. |
| D09X-1 | Backup dump block framing (`\x1e`-terminated JSON blocks, header signature `osTicket-Backup`, metadata record: version/prefix/salt/db-type/streams, table/row/end-table blocks) | FS-090.27 (exporter — canonical format owner) + BS-090.14 | FS-092.14/FS-092.15/§Data Req restate the block grammar for the *consume* side | S | KEEP — FS-092 explicitly cedes format to FS-090.27 and documents restore only; the restated grammar is necessary for the importer contract. Well-bounded. No trim. |
| D09X-2 | Install-time `display_errors=on` tuning (the value the packager inverts) | FS-060.1 (installer bootstrap) / FS-001 (runtime hardening) | FS-092.12/BS-092-08/KL-092-04 restate it as the inverse-transform reference | S | KEEP — FS-092 cross-refs FS-060.1/FS-001 and only documents the release-time inverse. Correct ownership. |
| D09X-3 | `INCLUDE_DIR` / `ROOT_DIR` / `main.inc.php` bootstrap constants | FS-001 (bootstrap) | FS-092.8/§Data Req document only the *rewrite* of the INCLUDE_DIR line | S | KEEP — FS-092 cedes the constants to FS-001, owns the deploy/unpack rewrite mechanic. Clean. |
| D050-1 | `Category::lookup` returns hollow object for any numeric id (no row-existence check) | FS-050 (KL-050.8 — most detailed, in the KB router context) AND FS-032 (note + KL-032.11) | FS-050 KL-050.8/EC-050.10 + FS-032 note@538 + KL-032.11 | S (both) | KEEP both — FS-032 frames it for the admin category form, FS-050 for the public/staff KB router; both are legit consumers of the same shared quirk. Each ~cross-refs. No merge needed; optionally add a mutual see-also. |
| D050-2 | FAQ category delete cascade (deleting category deletes its FAQ rows) | FS-032 (FS-032.16 — owns category lifecycle) | FS-050 BS-050.5 restates the cascade + adds the orphan-`faq_topic` quirk (KL-050.4) | S | KEEP — FS-050's interest is the *content-destruction* consequence + the topic-cleanup-skip bug (KL-050.4) which FS-032 does NOT cover. Both legitimate; FS-050 already says "Category delete is invoked from FS-032". No trim. |
| D050-3 | cannedResp AJAX method co-located under `kb` namespace / "No such premade reply" 404 | FS-022 (FS-022.14 — canned-response contract owner) | FS-050.10 last bullet + dep-table note it as co-located only | S | KEEP — FS-050 explicitly defers behavior to FS-022; co-location note is correct and minimal. |
| D050-4 | Help-topic `ispublic` flag gating public KB topic filter | FS-030 (help-topic entity owner) | FS-050.18/BS-050.7 reference it read-only | S | KEEP — FS-050 states "owned by FS-030 … referenced read-only here". Clean. |
| D050-5 | File attachment download URL/hash (`file.php?h=`, `md5(id+session_id+hash)` access key) | FS-022/FS-091 (file storage owner) | FS-050.9 restates the hash recipe; FS-022.14 also states `md5(file_id+session_id+file_hash)` | V (recipe) | KEEP-ish — minor VERBATIM overlap of the hash formula between FS-050.9 and FS-022.14. Low risk; optionally TRIM FS-050.9 to "per-session access key (FS-022)". Either acceptable. |

## Inverse check (content in MY specs that canonically belongs elsewhere)

- FS-060 carries the heaviest "borrowed canon": seed VALUE literals (D05X-1..4,6,7) that are
  FS-091's. These are the primary trim targets. FS-060's legitimate content is install-time
  *behavior* (binding-by-lookup, config rewrite order, admin-row creation, prefix-collision guard).
- FS-061 borrows the streams.cfg discovery rule from itself-as-owner but FS-060 also carries it
  verbatim (D06X-4) — trim FS-060.
- FS-050 cleanly defers Category CRUD, canned responses, help-topic entity, file storage, and
  config persistence. Its only verbatim leak is the file-hash recipe (D050-5, minor).
- FS-092 is exemplary — every cross-spec concern (dump format, THIS_VERSION, display_errors,
  INCLUDE_DIR/ROOT_DIR) is explicitly ceded with a cross-ref and only the local transform documented.

## Counts

- Total findings: 24
- VERBATIM (trim recommended): 8 — D05X-1, D05X-2, D05X-3, D05X-4, D05X-6, D05X-7, D06X-4, D050-5
- ACCEPTABLE summaries (keep, possibly add cross-ref): 16
- Recommended actual edits: trim FS-060 seed-value tables (D05X-1/2/3/4/6/7) + streams.cfg rule
  (D06X-4) to cross-refs; optional 1-line trim of FS-050.9 hash recipe (D050-5).
